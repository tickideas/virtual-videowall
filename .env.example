# Database (use Coolify's PostgreSQL service internal URL or public URL)
# For local: postgresql://user:password@localhost:5432/videowall?schema=public
# For Coolify standalone: use the postgres service public URL or internal network name
DATABASE_URL="postgresql://user:password@postgres-hostname:5432/videowall?schema=public"

# NextAuth (use your public domain)
# For local: http://localhost:3000
# For production: https://videowall.yourdomain.com
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="generate-with-openssl-rand-base64-32"

# Admin credentials (for initial setup)
ADMIN_EMAIL="admin@example.com"
ADMIN_PASSWORD="change-this-password"

# LiveKit (Server-side - use public URL for standalone services)
# IMPORTANT: For Coolify standalone services, use the PUBLIC LiveKit URL (wss://)
# Both server and client should use the same public URL
LIVEKIT_URL="wss://livekit.yourdomain.com"
LIVEKIT_API_KEY="LK41e534fcd2edbeb7"
LIVEKIT_API_SECRET="34VbPa4AuiBsVvThn9mKUH+gJ3Ugs9FXPznSsI3ThXg="

# LiveKit (Client-side - must be publicly accessible with WSS for HTTPS sites)
# MUST match LIVEKIT_URL for standalone deployments
NEXT_PUBLIC_LIVEKIT_URL="wss://livekit.yourdomain.com"

# App Settings
NEXT_PUBLIC_APP_NAME="Virtual Video Wall"
NEXT_PUBLIC_MAX_CHURCHES=60
